div(id=`deleteModal${post._id}` class="modal fade" tabindex="-1" role="dialog" aria-labelledby="deleteModalLabel" aria-hidden="true")
  .modal-dialog
    .modal-content
      .modal-header
        h5(id="deleteModalLabel" class="modal-title") Delete Post
        button(type="button" class="close" data-dismiss="modal" aria-label="Close")
          span(aria-hidden="true") &times;
      .modal-body
        p Are you sure you want to delete this post?
      .modal-footer
        button(type="button" class="btn btn-secondary" data-dismiss="modal") No, Cancel
        form(action=`/delete/${post._id}` method="POST")
          button(type="submit" class="btn btn-danger") Yes, Delete